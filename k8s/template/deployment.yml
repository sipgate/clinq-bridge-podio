apiVersion: apps/v1
kind: Deployment
metadata:
  name: $APP
  labels:
    app: $APP
spec:
  replicas: 2
  selector:
    matchLabels:
      app: $APP
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: $APP
    spec:
      containers:
        - name: $APP
          image: $IMAGE
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: 100m
              memory: 200Mi
            limits:
              cpu: 200m
              memory: 400Mi
          ports:
            - containerPort: 8080
          livenessProbe:
            initialDelaySeconds: 30
            httpGet:
              path: /health
              port: 8080
          readinessProbe:
            initialDelaySeconds: 30
            httpGet:
              path: /health
              port: 8080
          env:
            - name: REDIS_URL
              value: redis://clinq-cache-redis-master.redis.svc.cluster.local
            - name: OAUTH_IDENTIFIER
              value: PODIO
            - name: PODIO_CLIENT_ID
              value: clinq-nnhvbx
            - name: PODIO_REDIRECT_URL
              value: https://podio.bridge.clinq.com/oauth2/callback
            - name: PODIO_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: $APP
                  key: podioClientSecret
